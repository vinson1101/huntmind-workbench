# frontend_dev_batch_001

本批用于校准前端开发岗位的 shortlist / 建联决策。

## 当前状态
- 这是从 judge 现有 fixture 落盘出的正式 calibration batch。
- 目的不是扩样本，而是先让 judge 读取 `calibration_set`，不再走代码内置 fixture。

## 重点观察
- 前端开发不应误路由到 `product_manager`
- 研发模板边界应保持稳定
